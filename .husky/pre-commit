#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo ""
echo "🛡️  Clean Code Guard — checking standards before commit..."
echo "────────────────────────────────────────────────────────"
echo "📏  Max 20 lines per function  (aim for 10 — CLAUDE.md)"
echo "📄  Max 300 lines per file     (source files; tests exempt)"
echo "🤝  Max 3 parameters           (use options objects beyond 3)"
echo "🔀  Complexity ≤ 10            (use guard clauses to reduce nesting)"
echo "📦  1 class per file           (Single Responsibility)"
echo "🧹  No unused imports/code     (run: npm run lint:fix to auto-clean)"
echo "────────────────────────────────────────────────────────"
echo "💡  On failure: see CLEAN_CODE.md for fix strategies"
echo ""

npx lint-staged
npx tsc --noEmit
