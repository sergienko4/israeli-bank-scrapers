name: Rejected npm-pin workflow fixture
on: [push]

permissions:
  contents: read

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Install npm from a floating tag, global flag after the package
        run: |
          npm install npm@latest -g --ignore-scripts
