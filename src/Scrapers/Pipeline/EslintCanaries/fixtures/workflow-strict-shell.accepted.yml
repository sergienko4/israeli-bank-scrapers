name: Accepted fixture (strict shell present)

on:
  pull_request:

permissions:
  contents: read

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "shell defaults already enforce -euo pipefail; this run block stays bare."
