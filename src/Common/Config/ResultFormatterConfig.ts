/** Maximum number of transactions to show in the CLI result summary. */
export const MAX_TXN_PREVIEW = 3;

/** Width of the separator line in the result summary (character count). */
export const SEPARATOR_WIDTH = 40;

/** Number of trailing digits to show when masking account numbers. */
export const MASK_TAIL_LENGTH = 4;

/** Number of leading characters to show when masking transaction descriptions. */
export const DESC_PREVIEW_LENGTH = 3;
