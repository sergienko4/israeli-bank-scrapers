/** Minimum human-like delay before filling an input field (ms). */
export const FILL_INPUT_DELAY_MIN_MS = 200;

/** Maximum human-like delay before filling an input field (ms). */
export const FILL_INPUT_DELAY_MAX_MS = 600;

/** Minimum human-like delay before clicking a button (ms). */
export const CLICK_BUTTON_DELAY_MIN_MS = 200;

/** Maximum human-like delay before clicking a button (ms). */
export const CLICK_BUTTON_DELAY_MAX_MS = 800;

/** Polling interval when waiting for an iframe to appear (ms). */
export const IFRAME_POLL_INTERVAL_MS = 1000;

/** Default timeout for waiting for an iframe to appear (ms). */
export const IFRAME_DEFAULT_TIMEOUT_MS = 30000;

/** Maximum characters to capture from page body text for diagnostics. */
export const PAGE_TEXT_CAPTURE_LIMIT = 400;

/** Maximum characters to capture from an element's outer HTML for diagnostics. */
export const ELEMENT_HTML_CAPTURE_LIMIT = 300;
