/** Default timeout for waiting for a URL redirect (ms). */
export const REDIRECT_TIMEOUT_MS = 20000;

/** Polling interval for URL change detection (ms). */
export const URL_POLL_INTERVAL_MS = 1000;

/** Default timeout for waiting for a specific URL match (ms). */
export const URL_WAIT_TIMEOUT_MS = 20000;
