/** Default timeout for navigation-related URL waits (ms). */
export const NAVIGATION_TIMEOUT_MS = 20000;

/** Polling interval for URL change detection (ms). */
export const URL_POLL_INTERVAL_MS = 1000;
